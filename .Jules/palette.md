## 2024-03-12 - Pagination Accessibility
**Learning:** Icon-only buttons for pagination controls lack sufficient context for screen reader users and need explicit Polish translations (e.g., 'Poprzednia strona').
**Action:** Add `aria-label` to icon-only buttons and `aria-current` to active state buttons in pagination components.
