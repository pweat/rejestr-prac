## 2026-06-17 - Add ARIA labels to icon-only buttons
**Learning:** Icon-only close and delete buttons throughout the application lack context for screen readers. Polish translations are needed for these specific generic components.
**Action:** Always add `aria-label="Zamknij"` to close buttons (`&times;`) and `aria-label="Usuń wpis"` to delete buttons (`×`).
